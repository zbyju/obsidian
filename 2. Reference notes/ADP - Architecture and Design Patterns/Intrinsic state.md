It is the internal data of an object that is independent of the object's context - it does not change across instances.

In the context of [[Flyweight pattern]] it is stored inside the flyweight objects.